{% extends 'main.html.twig' %}

{% block title %}{{ 'calculator.calculate.meta_title'|trans }}{% endblock %}

{% block body %}
    <div class="page-heading">
{#        <a class="btn btn-secondary me-1 mb-1 float-end" href="{{ path('app_mail_segment_index') }}">{{ 'common_trans.back'|trans }}</a>#}
        <h3>{{ block('title') }}</h3>
    </div>
    <div class="page-content">
        <section id="calculator">
            <div class="row match-height">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h4 class="card-title">Calculator Card</h4>
                        </div>
                        <div class="card-content">
                            <div class="card-body">
                                {{ component('player_hand_calculator_form') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
